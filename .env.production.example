# ==================================
# VERCEL PRODUCTION ENVIRONMENT VARIABLES
# ==================================
# Copy these variables to your Vercel Dashboard -> Settings -> Environment Variables

# Server Configuration
PORT=3000
NODE_ENV=production

# Database (MongoDB Atlas recommended)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/echoes?retryWrites=true&w=majority

# JWT Configuration (generate with: openssl rand -base64 32)
JWT_SECRET=your-super-secret-jwt-key-here-make-it-long-and-complex

# Cloudinary Configuration (for file uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# CORS Origins (will be automatically set to your Vercel domain)
FRONTEND_URL=https://your-app-name.vercel.app

# ==================================
# VERCEL SPECIFIC VARIABLES
# ==================================
# These are automatically provided by Vercel, but you can override if needed
# VERCEL_URL (automatically provided)
# VERCEL_ENV (automatically provided)

# ==================================
# SOCKET.IO CONFIGURATION
# ==================================
# For Socket.IO to work properly on Vercel
SOCKET_IO_TRANSPORTS=websocket,polling
SOCKET_IO_CORS_ORIGIN=https://your-app-name.vercel.app
